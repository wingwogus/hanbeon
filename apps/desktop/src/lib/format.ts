/**
 * 화면에 숫자를 적는 방법.
 *
 * 프로필 모듈에 두지 않는다. floating 컨트롤러가 속도 한 줄을 적자고
 * 커맨드 호출 코드까지 끌어오게 되고, 그 창은 오프라인·저사양에서 항상 떠
 * 있어야 하는 화면이다.
 */

/** 밀리초를 사용자에게 보여줄 초 단위 문구로. */
export const formatSeconds = (ms: number) => `${(ms / 1000).toFixed(1)}초`
