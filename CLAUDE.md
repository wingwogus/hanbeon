# CLAUDE.md

'한번(HanBeon)' — 상황적응형 싱글스위치 접근성 소프트웨어.
제7회 국립재활원 보조기기 해커톤 출품작, 팀 데브파이브.

제품 요구사항은 [`docs/PRD.md`](docs/PRD.md)에 있다. 기능을 만들기 전에 반드시 PRD의
해당 항목을 먼저 읽는다. PRD와 코드가 어긋나면 PRD를 고치거나 코드를 고치되,
둘 중 하나를 방치하지 않는다.

## 이 제품이 푸는 문제

기존 싱글스위치 자동주사는 **커서가 대상 요소에 도달할 때까지 기다렸다가** 스위치를
누르는 방식이다. 긴 목록에서 목표를 지나치면 한 바퀴를 다시 기다려야 한다.

'한번'은 스캔 대상을 **웹페이지의 요소가 아니라 4개의 동작**으로 바꾼다.

| 버튼    | 주입하는 키 | 의미            |
| ------- | ----------- | --------------- |
| `>`     | `Tab`       | 다음 요소로     |
| `<`     | `Shift+Tab` | 이전 요소로     |
| `Enter` | `Enter`     | 선택            |
| `설정`  | —           | 설정 화면 진입  |

순환 대상이 소수로 고정되므로 최악의 대기가 몇 틱으로 한정되고, **지나쳐도 `<`로
되돌아갈 수 있다.**

순환에서 `Enter`는 **이동 칸마다 뒤에 한 번씩 끼어든다** — `>` → `Enter` → `<` →
`Enter` → … 옮긴 뒤에 고르는 흐름이 가장 잦아서, 둘이 멀면 그 흐름이 매번 한
바퀴를 돈다. 같은 이유로 **선택하면 첫 이동 칸부터 다시 시작한다.** 그래서 칸은
4개여도 기본 순환 자리는 5개다.

앱에 따라 이 4칸 **뒤에** 최대 3칸이 더 붙는다(PRD F11). PDF 뷰어의 페이지 넘김,
음악 앱의 재생 제어처럼 `Tab`·`Enter`로는 아예 닿을 수 없는 동작이 있기 때문이다.
여기에 두 가지 선이 있다.

- **앞 4칸의 순서와 자리는 어떤 앱에서도 바뀌지 않는다.** 사용자는 자리로 동작을
  기억한다.
- **세트는 포그라운드 앱이 바뀔 때만 교체한다.** 커서 위치나 화면 요소에 따라
  실행 중에 칸이 바뀌면 다음에 무엇이 올지 예측할 수 없고, 그 순간 이 제품이
  기존 자동주사보다 나은 이유가 사라진다.

이 두 선을 넘는 변경은 제품의 핵심 가설을 깨는 것이므로 PRD 변경 없이 하지 않는다.

## 레포 구조

```
apps/desktop/          Tauri 앱 — 이 프로젝트의 본체
  src/                 Next.js (static export) 프론트
    app/page.tsx       floating 컨트롤러 창
    app/settings/      설정 창
  src-tauri/           Rust 코어
    src/lib.rs         엔트리, Tauri 커맨드
    src/window.rs      floating 창 배치, non-activating 처리
apps/front/            랜딩·사용설명서 웹 (Next.js)
apps/admin/            실증 로그·정량 지표 대시보드 (Next.js)
apis/api/              사용자 프로필 동기화 API (Rust / vespera / sea-orm)
docs/PRD.md            제품 요구사항
```

Bun 워크스페이스(`apps/*`) + Cargo 워크스페이스(`apis/api`, `apps/desktop/src-tauri`)가
한 레포에 겹쳐 있다. Rust 빌드 산출물은 루트 `/target`에 모인다.

## 명령어

```sh
bun install
bun run desktop         # Tauri 개발 모드 (프론트 dev 서버까지 함께 뜬다)
bun run desktop:build   # 플랫폼 번들
bun run api             # 프로필 동기화 API
bun run dev             # front / admin 웹

bun run summary         # 실증 기록을 지표로 접어 출력 (기본: OS 로그 폴더)
bun run summary <파일|폴더> [--json]

bun run lint            # oxlint + cargo clippy + cargo fmt --check
bun run lint:fix
bun run test            # bun test + cargo tarpaulin
```

커밋 전 `bun run lint`는 husky pre-commit에서 자동으로 돈다.
릴리스 노트는 `bun run changepacks`로 관리한다.

## 프론트엔드 규약

스타일은 **`@devup-ui/react` 하나로만** 한다. CSS 파일, `style={{}}`, Tailwind를 새로
들이지 않는다.

- 색은 반드시 `apps/desktop/devup.json`의 토큰(`$primary`, `$scanCursorBg`, `$title` …)을
  참조한다. 하드코딩된 hex를 컴포넌트에 쓰지 않는다.
- 글자 크기는 `typography="switchLabel" | "h1" | "bodyL" | ..."` 토큰으로 지정한다.
- JSX prop은 알파벳 순으로 정렬한다 (oxlint가 강제한다).
- 레이아웃은 `Box` / `Flex` / `VStack` / `Center` / `Text`를 쓴다.
- 테마는 `light` / `dark` / `contrast` 세 가지다. **새 컴포넌트는 세 테마에서 모두
  확인한다.** `contrast`는 저시력 사용자용 흑배경·노랑 커서 조합이라 회색 계열
  중간톤을 쓰면 그대로 안 보이게 된다.

**스타일 값은 리터럴로 적는다.** devup-ui는 빌드 때 값을 뽑아 CSS로 만드는데,
상수나 변수를 넘기면 뽑아내지 못하고 정의되지 않는 CSS 변수(`var(--f)` 같은)로
바꿔 놓는다. 규칙이 통째로 무효가 되면서 화면에서만 티가 나므로, 공통 값을 상수로
빼고 싶어질 때마다 빌드된 CSS를 확인한다.

### 글꼴

Tauri 앱은 오프라인에서 동작해야 한다. **CDN 폰트·외부 이미지·런타임 네트워크
의존을 프론트에 넣지 않는다.** (`apps/front`, `apps/admin`은 웹이므로 예외)

그래서 Pretendard를 `apps/desktop/public/fonts/`에 넣어 함께 배포하고,
`app/layout.tsx`의 `globalCss`에서 `@font-face`로 등록한다. `@font-face`는 객체가
아니라 태그드 템플릿(원본 CSS) 형태로만 넘어간다.

**글꼴 이름은 `layout.tsx` 한 곳에서만 정한다.** `devup.json`의 typography 토큰에
`fontFamily`를 넣지 않는다 — 넣으면 그 클래스가 전역 규칙을 덮어써서 대체 글꼴이
통째로 무시되고, 폰트를 읽지 못한 기기에서 브라우저 기본 명조로 떨어진다.
작은 크기의 명조는 저시력 사용자에게 특히 불리하다.

**두 창이 `app/layout.tsx`의 전역 CSS를 함께 쓴다.** floating 창만 생각하고 규칙을
넣으면 설정 창이 조용히 망가진다 — `html, body`에 `overflow: hidden`을 걸어 두어
설정 항목이 늘어난 뒤로 화면 아래쪽에 아예 닿을 수 없던 적이 있다. 전역 규칙을
건드릴 때는 설정 창에서도 확인한다.

Next.js는 `output: 'export'`다. 서버 컴포넌트의 데이터 패칭, 라우트 핸들러,
미들웨어, `next/image` 최적화는 쓸 수 없다. 새 창을 추가하면
`out/<경로>/index.html`이 생기고, 이를 `tauri.conf.json`의 `windows[].url`에 적는다.

## Rust 코어 규약

**스캔 상태기계·입력 판정·키 주입은 Rust에 둔다.** 프론트는 코어가 emit한 상태를
그리기만 한다. 주사 간격이 렌더링 지연에 흔들리면 안 되고, 스위치 눌림 시간 판정과
키 주입이 같은 쪽에 있어야 지연을 예측할 수 있기 때문이다.

- 새 crate는 반드시 `cargo add`로 추가한다. 버전을 손으로 적어 넣지 않는다.
- Tauri 커맨드는 `Result<T, String>`을 반환하고, 에러 문구는 한국어로 사용자에게
  그대로 보여줄 수 있게 쓴다.
- 새 커맨드·플러그인을 추가하면 `src-tauri/capabilities/default.json`에 권한을
  같이 등록한다. 빠뜨리면 런타임에 조용히 거부된다.

### floating 창이 포커스를 뺏으면 안 된다

이 앱의 가장 깨지기 쉬운 지점이다. floating 창이 활성화되는 순간, 우리가 주입한
`Tab`이 대상 앱이 아니라 우리 창으로 들어가 스캔이 무의미해진다.

- macOS: `NSPanel` + `NonactivatingPanel` 스타일 마스크, activation policy `Accessory`
- Windows: `WS_EX_NOACTIVATE`

**둘 다 아직 목표 상태다.** 지금 macOS는 `Accessory` 정책만 걸려 있고 NSPanel
승격은 하지 않았으며, Windows의 `WS_EX_NOACTIVATE`는 TODO로 남아 있다
(`window::make_non_activating`). 그래서 **창을 클릭하는 순간 우리 앱이 활성 앱이
된다.** 드래그로 창을 옮길 수 있게 되면서 이 경로가 실제로 열렸고, 이동이 멎으면
`window::release_activation`이 활성 상태를 직전 앱에 돌려주는 것으로 막고 있다.
창을 클릭하게 만드는 UI를 추가할 때는 이 복귀 경로를 함께 태운다.

창을 보이거나 옮기거나 크기를 바꾸는 코드를 추가할 때마다 **실제로 다른 앱
(브라우저·문서 편집기)에 포커스를 둔 채로** 동작을 확인한다. 개발 중 프론트만 보고
넘어가면 반드시 놓친다.

### 설정 창은 요구가 정반대다

macOS는 앱 전체에 `ActivationPolicy::Accessory`를 걸어 floating 창이 활성 앱을
가로채지 않게 한다. 그런데 **설정 창은 조작을 받아야 하므로 반드시 활성화되어야
한다.** Accessory인 채로 열면 창은 보이지만 포커스가 가지 않아 아무것도 할 수 없다
(M1에서 실제로 이 상태였다).

그래서 `window::show_settings`는 정책을 `Regular`로 올리고, `hide_settings`는
`Accessory`로 되돌린다. 설정 창의 닫기 버튼은 `on_window_event`에서 가로채
`prevent_close` 후 `hide_settings`를 태운다. **이 경로를 우회해 창을 닫으면
정책이 Regular로 남아 floating 컨트롤러가 포커스를 뺏기 시작한다.**

### 대상 앱의 포커스 요소 읽기 (macOS)

가림 판정(`occlusion.rs`)은 '지금 포커스를 가진 요소'의 화면 좌표를 접근성
API로 읽는다. 두 가지를 모르면 시간을 크게 버린다.

- **시스템 전역 요소는 막혀 있다.** `AXUIElementCreateSystemWide`로 묻는 길은
  최근 macOS에서 권한이 있어도 `kAXErrorCannotComplete`를 돌려준다. 활성 앱의
  **pid로 직접** `AXUIElement`를 만들어야 값이 나온다.
- **활성 앱이 우리 자신이면 건너뛴다.** 설정 창을 열면 포커스 요소로 우리
  웹뷰가 잡히고, 그러면 창이 자기 자신을 가린다고 판정해 영영 흐려진다.

좌표는 논리 px(주 화면 좌상단 원점)로 오고, 창 좌표는 물리 px로 온다.
배율로 나눠 맞춘 뒤에 비교한다.

### macOS 접근성 권한

키 주입에는 손쉬운 사용 권한이 필요하다. 개발 모드에서는 앱이 아니라 **터미널/IDE**에
권한을 줘야 한다. 권한이 없으면 키 주입이 에러 없이 무시되므로, 코드를 의심하기 전에
권한부터 확인한다.

### 개발용 환경변수

`HANBEON_SWITCH_KEY`(스위치 키, 기본 `F13`), `HANBEON_INTERVAL_MS`(시작 주사 간격),
`HANBEON_SOUND=off`(소리 끄기), `HANBEON_LOG=1`(상태 전이 출력).
모두 개발용 통로이며 사용자용 설정은 M3에서 프로필로 옮긴다.

**타이밍에 의존하는 검증을 할 때는 `HANBEON_INTERVAL_MS`를 크게 잡고
`HANBEON_LOG=1`을 켠다.** 화면만 보고 "동작하지 않는다"고 판단하면 틀린다 —
`<`(Shift+Tab)는 텍스트 편집기에서 아무 흔적도 남기지 않아서, 정상 동작 중인
머무름을 실패로 오인하기 쉽다.

하드웨어 스위치가 없을 때는 `HANBEON_SWITCH_KEY=Space`로 스페이스바를 스위치
삼아 검증한다. **검증이 끝나면 반드시 앱을 종료한다** — 전역 단축키로 잡혀 있는
동안에는 모든 앱에서 스페이스바가 먹히지 않는다.

### 자동화로 검증할 때의 함정 (macOS)

`osascript`로 키를 보내 검증할 수 있지만, 두 가지를 알고 시작해야 한다.

- **권한이 조용히 사라진다.** 키가 전송되지 않아도 에러가 없을 때가 있다.
  앱을 의심하기 전에 TextEdit에 `keystroke "AB"`를 보내 실제로 입력되는지
  먼저 확인한다. 이걸 건너뛰면 멀쩡한 코드를 몇 시간 파게 된다.
- **UI 스크립팅(메뉴·버튼 클릭)은 건드리지 않는다.** 키 전송과 별개 권한이라
  요청 대화상자가 뜨고, 그 과정에서 기존 키스트로크 권한까지 리셋된다.
  설정 화면처럼 UI 조작이 필요한 검증은 사람이 직접 하는 편이 빠르다.

### 실증 기록

무엇이 언제 일어났는지를 `journal`이 JSON Lines로 남긴다(PRD 10.1). 정량 지표를
실측으로 주장하려면 이 기록이 근거가 된다.

- **쓰기는 별도 스레드에서 한다.** 커서 이동은 주사 간격마다 일어나므로 그
  자리에서 파일에 쓰면 간격이 흔들린다. 기록 지점을 늘릴 때 이 선을 넘지 않는다.
- **기록은 이 기기 밖으로 나가지 않는다.** 코어에 전송 코드를 두지 않는다.
- 사건에 코어의 타입을 그대로 넣지 않는다. 타입이 바뀌면 지난 기록을 읽을 수
  없게 된다 — 문자열·숫자로만 적는다.
- 지표 계산은 `apps/admin/src/lib/metrics.ts` **한 곳**에 둔다. CLI(`bun run
  summary`)와 대시보드(`/dashboard`)가 같은 함수를 쓴다 — 계산이 두 벌이 되면
  화면의 숫자와 보고서의 숫자가 갈린다.
- 대시보드는 기록 파일을 **브라우저 안에서만** 읽는다. 업로드 경로를 만들지
  않는다.

차트를 그릴 때는 색을 눈으로 고르지 않는다. 마크 색이 표면과 충분히 대비되는지
계산해서 정한다 — 브랜드 보라는 다크 표면에서 2.95:1이라 읽히지 않아, 같은
색상의 밝은 단계를 `chartAccent` 토큰으로 따로 두었다.

### 사용자 프로필

`profile.json`(OS별 설정 폴더)에 저장하고 시작 시 복원한다. `Profile::sanitize`가
모든 진입점에서 값을 성립 가능한 범위로 되돌린다 — 손상된 파일이나 옛 버전이 남긴
값으로 스캔이 멈추거나 폭주하면 사용자는 앱을 조작할 수단을 완전히 잃는다.

적응으로 조정된 간격은 조정될 때마다 파일에 적는다. 종료 시에만 저장하면
강제 종료(`SIGTERM` 포함)에서 사용자가 익숙해진 속도를 잃는다.

## 접근성 원칙 (기능보다 우선한다)

이 소프트웨어의 사용자는 실수 한 번의 비용이 매우 크다. 아래는 협상 대상이 아니다.

1. **수동이 자동을 이긴다.** 적응 로직은 사용자가 정한 최소·최대 간격 범위를 절대
   벗어나지 않는다. 수동 고정이 켜져 있으면 어떤 근거로도 간격을 바꾸지 않는다.
2. **자동 변경은 항상 보인다.** 주사 간격이 바뀌면 현재 값과 변경 이유를 화면에
   표시한다. 이유를 알 수 없는 변화는 사용자의 통제감을 무너뜨린다.
3. **모든 선택은 3초 안에 되돌릴 수 있다.** 되돌리기가 불가능한 동작을 스캔 대상에
   추가하지 않는다.
4. **상태는 두 감각으로 알린다.** 화면 강조 + 소리(또는 LED). 소음 환경과 저시력
   상황 중 어느 하나만 가정하지 않는다.
5. **모션을 남용하지 않는다.** 커서 이동에 트랜지션·애니메이션을 넣지 않는다.
   `prefers-reduced-motion`은 전역에서 이미 차단한다.
6. **활성 상태를 색만으로 표현하지 않는다.** 테두리 두께·크기 등 색 이외의 단서를
   반드시 함께 준다.
7. **설정은 한 화면에 펼친다.** 계층 메뉴를 만들지 않는다. 초기 설정 10분, 일상
   조정 한 화면이 목표 지표다.

## 플랫폼 범위

MVP는 **macOS + Windows**다.

iOS/iPadOS는 서드파티 앱이 전역 키 입력 주입이나 시스템 오버레이를 할 수 없어,
'앱이 다른 앱을 제어'하는 구조 자체가 OS 정책상 불가능하다. 태블릿은 하드웨어
스위치를 BT HID로 붙여 OS 내장 '스위치 제어'와 연동하고, 우리 앱은 설정·프로필
제공에 한정한다. **이 제약을 우회하는 방법을 찾으려 시도하지 않는다.**
Android(AccessibilityService + 오버레이)와 Linux는 MVP 이후 과제다.

## 하지 말 것

- 앞 4칸의 순서·자리를 바꾸거나, 실행 중에 스캔 대상을 갈아 끼우기
- 하드코딩된 색·글자 크기, `style={{}}`, 새 스타일 라이브러리 도입
- Rust crate 버전 손으로 적기
- floating 창 관련 변경을 프론트에서만 확인하고 넘어가기
- 사용자 설정을 코드가 임의 판단으로 덮어쓰기
- 실측 없이 정량 지표(성공률·오선택 감소율) 달성을 주장하기
- 스위치로 앱을 전환하는 기능 넣기 — MVP는 **보호자가 앱을 띄워 준다**고
  전제한다(PRD 5.4). 앱 목록을 스캔하는 순간 4칸 구조와 부딪힌다.

## 팀

| 담당   | 영역                                                                  |
| ------ | --------------------------------------------------------------------- |
| 오정민 | 기획·접근성 실증, 요구사항, QA·정량 지표, 통합 검수                   |
| 우수연 | 소프트웨어 — Rust 코어(스캔·입력 판정·키 주입), 데스크톱 UI, 프로필 API |
| 이태현 | 하드웨어 — 대형 스위치·하우징, 펌웨어, USB/BT HID 연결                |

세 명이라 한 사람이 여러 층을 본다. **소프트웨어와 하드웨어의 경계에서만
합의가 필요하다** — 그 경계가 아래 가정이다.

하드웨어는 별도 트랙으로 제작 중이다. 소프트웨어는 스위치가 **USB/BT HID
키보드로서 특정 키를 보낸다**고 가정하고 그 키를 전역 수신한다. 이 가정이
바뀌면 PRD의 '하드웨어 인터페이스' 절을 먼저 갱신한다.
